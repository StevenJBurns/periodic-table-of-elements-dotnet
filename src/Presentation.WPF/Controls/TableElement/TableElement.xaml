<UserControl
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  x:Class="SJB.Science.PeriodicTable.View.PeriodicTableElement"
  mc:Ignorable="d" d:DesignHeight="64" d:DesignWidth="64"
  >
  <Grid Margin="2">
    <Grid.ColumnDefinitions />
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="100*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Grid.Resources>
      <Style TargetType="Label">
        <Setter Property="FontSize" Value="10" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
      </Style>
    </Grid.Resources>
    <Label Grid.Row="0" Content="Atomic #" HorizontalContentAlignment="Left" />
    <Label Grid.Row="1" FontSize="16" Content="Symbol" />
    <Label Grid.Row="2" Content="Atomic Weight" />
    <Label Grid.Row="3" FontWeight="Normal" Content="ElementName" />
  </Grid>
</UserControl>
